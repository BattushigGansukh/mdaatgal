<style>
  body {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0;
    background: #f4f4f4;
    font-family: 'Calibri', Arial, sans-serif;
  }
.form-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32px;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 4px 24px rgba(0,0,0,0.07);
  max-width: 400px;
}
body, .form-container {
color: #0047ba;
}

.logo-wrapper {
  text-align: center;
  margin-bottom: 18px;
}

.site-logo {
  max-height: 60px;
  width: auto;
}

h2 {
  margin-top: 0;
  color: #0275d8;
  text-align: center;
  font-size: 2em;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.6em;
}

.icon {
  display: inline-flex;
  vertical-align: middle;
  margin-right: 8px;
}

.icon img {
  width: 1.2em;
  height: 1.2em;
  object-fit: contain;
}

label {
  display: block;
  margin-bottom: 8px;
  font-weight: 600;
  color: #333;
}

input, textarea, select {
  width: 100%;
  padding: 9px 12px;
  margin-bottom: 18px;
  border: 1px solid #ccc;
  border-radius: 4px;
  font-size: 1em;
  box-sizing: border-box;
  transition: border-color 0.2s;
}

input:focus, textarea:focus, select:focus {
  border-color: #0275d8;
  outline: none;
}

/* Make the form section aligned left in a single column */
#form-section {
  text-align: left;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

#submittal-form {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  width: 100%; /* or set a max-width if desired */
}

#submittal-form label,
#submittal-form input,
#submittal-form textarea,
#submittal-form select,
#submittal-form button {
  margin-bottom: 12px;
  width: 100%; /* makes inputs and labels expand the full width of the container */
  box-sizing: border-box;
}

#submittal-form input[type="file"] {
  width: auto; /* file input looks better at auto width */
}

button {
  background: #0275d8;
  color: white;
  border: none;
  padding: 12px 0;
  width: 100%;
  border-radius: 4px;
  cursor: pointer;
  font-size: 1.1em;
  font-weight: bold;
  transition: background 0.2s;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.6em;
}

button:hover {
  background: #025aa5;
}
</style>
